<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luy.teaism">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/home_title"
            android:launchMode="singleTop"
            android:theme="@style/AppTheme.NoActionBar.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.setting.MeActivity"
            android:label="@string/me_title" />
        <activity
            android:name=".ui.ScanActivity"
            android:label="@string/scan_title" />
        <activity
            android:name=".ui.SearchActivity"
            android:label="@string/search_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.setting.SignInActivity"
            android:label="@string/sign_in_title" />
        <activity
            android:name=".ui.setting.SignUpActivity"
            android:label="@string/sign_up_title" />
        <activity
            android:name=".ui.setting.SettingActivity"
            android:label="@string/setting_title" />
        <activity
            android:name=".ui.StarActivity"
            android:label="@string/star_title"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.module.TeaFoodDetailActivity" />
        <activity
            android:name=".ui.NewArticleActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.module.CreateCommentActivity"></activity>
    </application>

</manifest>